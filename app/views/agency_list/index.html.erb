<div class="page-wrapper">
  <%= render "layouts/header" %>
  <div class="page-container">
    <%= render "layouts/sidebar" %>
    <div class="page-content-wrapper">
      <div class="page-content">
        <div class="page-bar">
          <div class="page-title-breadcrumb">
            <div class="pull-left">
              <div class="page-title">Agency List</div>
            </div>
            <%= render "layouts/breadcrumbs" %>
          </div>
        </div>
        <div class="row">
          <div class="col-md-12">
            <div class="row">
              <div class="col-sm-12">
                <div class="card">
                  <div class="card-body">
                    <% if flash[:error] %>
                      <div class="alert <%= session[:isErrorhandled].to_i > 0 ? 'alert-danger' : 'alert-success' %>" style="width:900px;" id="successXMessage">
                        <span class="closebtn" onclick="this.parentElement.style.display='none';">&times;</span>
                        <%= flash[:error] %>
                      </div>
                    <% end %>
                    <input type="hidden" id="rootXPath" value="<%= root_url %>"/>                                        
                    <input type="hidden" name="server_request" id="server_request" value="Y"/>
                    <%= form_tag({:action=>'index'} ,method: :get ,:multipart=>true,:id=>'myForms') do  %>
                      <div class="row m-b-10">
                        <label class="col-sm-12 col-md-2">Agency Name</label>
                        <div class="col-sm-12 col-md-3">
                          <input type="text" class="form-control" name="agency" id="agency" autocomplete="off" placeholder="Enter Agency Name" value="<% if @agency_list_search %><%=@agency_list_search%><% end %>"/>
                        </div>
                        <div class="col-sm-12 col-md-1">
                          <button type="button" class="btn btn-circle btn-default" onclick="filter_agency_list();">
                            <i class="fa fa-search"></i>
                          </button>
                        </div>
                        <div class="col-sm-12 col-md-6">
                          <a href="<%= root_url %>agency_list/referesh_agency_list" class="float-right">
                            <button type="button" class="btn btn-circle btn-info">
                              <i class="fa fa-refresh"></i> Reset
                            </button>
                          </a>
                        </div>
                      </div>
                    <% end %>
                    <div class="row">
                      <div class="col-sm-12 col-md-3">
                        <a href="<%= root_url %>agency_list/add_agency" class="">
                          <button type="button" class="btn btn-circle btn-success">
                            <i class="fa fa-plus"></i> Add Agency Code (Alpha / Numeric)
                          </button>
                        </a>
                      </div>
                      <div class="col-sm-12 col-md-9 hidden">
                        <a href="javascript:;" target="_blank" class="float-right" data-placement="bottom" data-toggle="tooltip" title="" data-original-title="Click for excel download">
                          <button type="button" class="btn dark btn-outline m-b-10" style="float:right;">CSV</button>
                        </a>
                      </div>
                    </div>
                    <div class="table-scrollable">
                      <table class="table">
                        <thead>
                          <tr>
                            <th>S.No</th>
                            <th>Agency Code</th>
                            <th>Agency Description</th>
                            <th>Active</th>
                            <th>Action</th>
                          </tr>
                        </thead>
                        <tbody>
                           <% pagestart = params[:page].present? ? params[:page].to_i : 1 %>
                          <% i = pagestart <= 1 ? 1 : ((pagestart - 1) * 10) + 1 %>

                          <% if @agency_list.any? %>
                            <% @agency_list.each do |agency| %>                  
                              <tr>
                                <td><%= i %></td>
                                <td><%= agency.Agency_Short %></td>
                                <td><%= agency.Agency_Description %></td>
                              <td><%= agency.Agency_Isactive == 'Y' ? 'Y' : 'N' %></td>
                                <td>
                                  <a href="<%= root_url %>agency_list/add_agency/<%= agency.Id %>?page=<%=params[:page] %>" class="tblEditBtn">
                                    <i class="fa fa-pencil"></i>
                                  </a>
                                  <a class="tblDelBtn" onclick="alertChecked('<%= root_url %>agency_list/<%= agency.Id %>/deletes')">
                                    <i class="fa fa-trash-o"></i>
                                  </a>
                                </td>
                              </tr>
                            <% end %>
                          <% else %>
                            <tr>
                              <td colspan="5" class="text-center">No records found</td>
                            </tr>
                          <% end %>
                        </tbody>
                      </table>
                    </div>
                    <div class="col-md-12 text-center">
                     <%= will_paginate @agency_list %>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
